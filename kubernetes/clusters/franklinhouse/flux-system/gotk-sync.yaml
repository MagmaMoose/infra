# This manifest was generated by flux. DO NOT EDIT.
# Repointed from CalebSargeant/infra-v2 to CalebSargeant/infra when franklinhouse
# was folded into the monorepo — see .claude/decisions/2026-08-13-franklinhouse-into-monorepo.md.
# A `flux bootstrap` re-run would re-emit this file; the url/path edits below have
# the same effect.
---
apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: flux-system
  namespace: flux-system
spec:
  interval: 10m
  ref:
    branch: main
  secretRef:
    name: flux-system
  url: ssh://git@github.com/CalebSargeant/infra
---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: flux-system
  namespace: flux-system
spec:
  interval: 10m
  path: ./kubernetes/clusters/franklinhouse/flux-system
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  decryption:
    provider: sops
    secretRef:
      name: sops-keys
