# This manifest was generated by flux. DO NOT EDIT.
# Repointed from calebsargeant/infra-v2 to MagmaMoose/infra when franklinhouse was
# folded into the monorepo — see docs/reference/franklinhouse.md. A `flux bootstrap`
# re-run would re-emit this file; the url/path edits below have the same effect.
#
# NOTE: MagmaMoose/infra is the CANONICAL name for this repo. firefly's
# gotk-sync.yaml still says github.com/CalebSargeant/infra, which only works
# because GitHub redirects the old owner path. That redirect breaks the moment
# anyone creates a repo at CalebSargeant/infra, so new clusters use the canonical
# name. firefly should be migrated separately.
---
apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: flux-system
  namespace: flux-system
spec:
  interval: 10m
  ref:
    branch: main
  secretRef:
    name: flux-system
  url: ssh://git@github.com/MagmaMoose/infra
---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: flux-system
  namespace: flux-system
spec:
  interval: 10m
  path: ./kubernetes/clusters/franklinhouse/flux-system
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  decryption:
    provider: sops
    secretRef:
      name: sops-keys
