apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - helmrelease.yaml
  # BackupTarget + RecurringJobs. There were no backups at all before this.
  - backup.yaml
  # Tiered StorageClasses + the node tags they select on. Neither was in Git before.
  - storageclasses.yaml
  - node-tags.yaml
