---
# Consolidated image-update automation for the media stack — third-party apps
# (Sonarr/Radarr/Plex/etc.) that live entirely in THIS repo and all push tag
# bumps to infra's main. Unlike the self-managed apps (zoey, buxfer-sync, …),
# which each own their source repo and carry their automation in
# apps/<app>/base/<app>/, these share one ImageUpdateAutomation here.
#
# Per-image ImageRepository + ImagePolicy (semver, current major held) stay
# flux-system-namespaced, so the workload markers remain `flux-system:<app>`:
#   image: linuxserver/sonarr:4.0.15 # {"$imagepolicy": "flux-system:sonarr"}
#
# Applied by the bootstrap `flux-system` Kustomization (this dir is referenced
# from clusters/firefly/flux-system/kustomization.yaml), same as the other
# shared Flux config here.

# ---------------------------------------------------------------------------
# Push source: the infra repo authenticated as the GitHub App (a `main` ruleset
# bypass actor). The bootstrap `flux-system` GitRepository uses the SSH deploy
# key, which is NOT a bypass actor — its direct pushes to main are declined by
# the branch ruleset. This App-authenticated source is what lets the bumps land.
# ---------------------------------------------------------------------------
apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: infra-image-automation
  namespace: flux-system
spec:
  interval: 10m
  provider: github
  ref:
    branch: main
  secretRef:
    name: buxfer-sync-github-app
  url: https://github.com/CalebSargeant/infra
---
# --- sonarr (linuxserver/sonarr) ---
apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImageRepository
metadata:
  name: sonarr
  namespace: flux-system
spec:
  image: linuxserver/sonarr
  interval: 1h
---
apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: sonarr
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: sonarr
  filterTags:
    pattern: '^[0-9]+\.[0-9]+\.[0-9]+$'
    extract: '$0'
  policy:
    semver:
      range: '>=4.0.0 <5.0.0'
---
# --- radarr (linuxserver/radarr) — held on 5.x; 6.x is a manual bump ---
apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImageRepository
metadata:
  name: radarr
  namespace: flux-system
spec:
  image: linuxserver/radarr
  interval: 1h
---
apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: radarr
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: radarr
  filterTags:
    pattern: '^[0-9]+\.[0-9]+\.[0-9]+$'
    extract: '$0'
  policy:
    semver:
      range: '>=5.0.0 <6.0.0'
---
# --- sabnzbd (linuxserver/sabnzbd) ---
apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImageRepository
metadata:
  name: sabnzbd
  namespace: flux-system
spec:
  image: linuxserver/sabnzbd
  interval: 1h
---
apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: sabnzbd
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: sabnzbd
  filterTags:
    pattern: '^[0-9]+\.[0-9]+\.[0-9]+$'
    extract: '$0'
  policy:
    semver:
      range: '>=4.0.0 <5.0.0'
---
# --- qbittorrent (linuxserver/qbittorrent) — bare tag only ---
apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImageRepository
metadata:
  name: qbittorrent
  namespace: flux-system
spec:
  image: linuxserver/qbittorrent
  interval: 1h
---
apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: qbittorrent
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: qbittorrent
  filterTags:
    pattern: '^[0-9]+\.[0-9]+\.[0-9]+$'
    extract: '$0'
  policy:
    semver:
      range: '>=5.0.0 <6.0.0'
---
# --- prowlarr (linuxserver/prowlarr) ---
apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImageRepository
metadata:
  name: prowlarr
  namespace: flux-system
spec:
  image: linuxserver/prowlarr
  interval: 1h
---
apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: prowlarr
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: prowlarr
  filterTags:
    pattern: '^[0-9]+\.[0-9]+\.[0-9]+$'
    extract: '$0'
  policy:
    semver:
      range: '>=2.0.0 <3.0.0'
---
# --- bazarr (linuxserver/bazarr) ---
apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImageRepository
metadata:
  name: bazarr
  namespace: flux-system
spec:
  image: linuxserver/bazarr
  interval: 1h
---
apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: bazarr
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: bazarr
  filterTags:
    pattern: '^[0-9]+\.[0-9]+\.[0-9]+$'
    extract: '$0'
  policy:
    semver:
      range: '>=1.0.0 <2.0.0'
---
# --- nzbhydra2 (linuxserver/nzbhydra2) ---
apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImageRepository
metadata:
  name: nzbhydra2
  namespace: flux-system
spec:
  image: linuxserver/nzbhydra2
  interval: 1h
---
apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: nzbhydra2
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: nzbhydra2
  filterTags:
    pattern: '^[0-9]+\.[0-9]+\.[0-9]+$'
    extract: '$0'
  policy:
    semver:
      range: '>=8.0.0 <9.0.0'
---
# --- overseerr (sctx/overseerr) — bare semver only (excludes git-sha tags) ---
apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImageRepository
metadata:
  name: overseerr
  namespace: flux-system
spec:
  image: sctx/overseerr
  interval: 1h
---
apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: overseerr
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: overseerr
  filterTags:
    pattern: '^[0-9]+\.[0-9]+\.[0-9]+$'
    extract: '$0'
  policy:
    semver:
      range: '>=1.0.0 <2.0.0'
---
# --- plex (lscr.io/linuxserver/plex) — public, anonymous scan ---
apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImageRepository
metadata:
  name: plex
  namespace: flux-system
spec:
  image: lscr.io/linuxserver/plex
  interval: 1h
---
apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: plex
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: plex
  filterTags:
    pattern: '^[0-9]+\.[0-9]+\.[0-9]+$'
    extract: '$0'
  policy:
    semver:
      range: '>=1.0.0 <2.0.0'
---
# --- flaresolverr (ghcr.io/flaresolverr/flaresolverr) — vX.Y.Z tags ---
apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImageRepository
metadata:
  name: flaresolverr
  namespace: flux-system
spec:
  image: ghcr.io/flaresolverr/flaresolverr
  interval: 1h
  provider: generic
  secretRef:
    name: ghcr-pull-secret
---
apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: flaresolverr
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: flaresolverr
  filterTags:
    pattern: '^v[0-9]+\.[0-9]+\.[0-9]+$'
    extract: '$0'
  policy:
    semver:
      range: '>=3.0.0 <4.0.0'
---
# --- n8n (n8nio/n8n) ---
apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImageRepository
metadata:
  name: n8n
  namespace: flux-system
spec:
  image: n8nio/n8n
  interval: 1h
---
apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: n8n
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: n8n
  filterTags:
    pattern: '^[0-9]+\.[0-9]+\.[0-9]+$'
    extract: '$0'
  policy:
    semver:
      range: '>=2.0.0 <3.0.0'
---
# Single image-update automation for every infra-hosted tag marker. Scans
# ./kubernetes/apps for `$imagepolicy` markers — the media workloads above, plus
# github-usage-dashboard (its chart is remote but its deploy tag lives here, with
# its ImagePolicy in apps/github-usage-dashboard/base/) — and pushes tag bumps to
# infra's main via the App-authenticated source above. Fully self-managed apps
# (nievah, git-pull-request-dashboard) keep their markers, and their own
# ImageUpdateAutomation, in their own repos, so they're out of scope here.
apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImageUpdateAutomation
metadata:
  name: media
  namespace: flux-system
spec:
  interval: 1h
  sourceRef:
    kind: GitRepository
    name: infra-image-automation
  git:
    checkout:
      ref:
        branch: main
    commit:
      author:
        email: actions@users.noreply.github.com
        name: fluxcdbot
      messageTemplate: |-
        chore(media): bump image tags
        {{range .Changed.Changes}}{{print .OldValue}} -> {{println .NewValue}}{{end}}

        [ci skip]
    push:
      branch: main
  update:
    path: "./kubernetes/apps"
    strategy: Setters
