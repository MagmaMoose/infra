apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: minio
  namespace: general-system
  labels:
    app.kubernetes.io/name: minio
spec:
  serviceName: minio
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: minio
  template:
    metadata:
      labels:
        app.kubernetes.io/name: minio
    spec:
      # `critical` (100000). Set DIRECTLY rather than via the placement label:
      # this is the data plane, and it must not depend on the Kyverno admission
      # webhook being healthy in order to avoid being preempted. The label
      # mechanism is admission-time only, so a pod admitted while Kyverno is down
      # (failurePolicy is Ignore) would land at priority 0 — which is precisely
      # the state that makes a datastore the scheduler's first eviction victim.
      # The CNPG Clusters set it directly for a related reason (their kind is not
      # matchable at all), so the whole data plane is consistent.
      priorityClassName: critical
      nodeSelector:
        kubernetes.io/hostname: ff-vm1
      containers:
        - name: minio
          image: quay.io/minio/minio:RELEASE.2024-12-18T13-15-44Z
          args:
            - server
            - /data
            - --console-address
            - ":9001"
          env:
            - name: MINIO_ROOT_USER
              value: admin
            - name: MINIO_ROOT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: minio-credentials
                  key: root-password
          ports:
            - containerPort: 9000
              name: api
            - containerPort: 9001
              name: console
          volumeMounts:
            - name: data
              mountPath: /data
          # 157Mi was OOM-killing MinIO (exit 137), which cascaded into the Loki
          # crashloop (chunk flushes failing with IncompleteBody/400). KRR rec ~181Mi;
          # give generous headroom for the critical object store. No CPU limit.
          resources:
            requests:
              cpu: 50m
              memory: 256Mi
            limits:
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /minio/health/live
              port: 9000
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /minio/health/ready
              port: 9000
            initialDelaySeconds: 10
            periodSeconds: 5
      volumes:
        - name: data
          hostPath:
            path: /mnt/raid/minio
            type: DirectoryOrCreate
