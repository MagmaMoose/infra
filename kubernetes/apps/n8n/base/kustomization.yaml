apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - deployment.yaml
  - service.yaml
  - ingress.yaml
  - pvc.yaml
  - secret.enc.yaml
  - externalsecret.yaml
components:
  # Resources set explicitly in deployment.yaml (raw, per repo convention) —
  # the c.small profile was overriding the inline values up to 512Mi.
  # Migrated off ff-pi1: hostPath pv.yaml dropped, pvc.yaml now Longhorn,
  # pinned to the worker node (k3s agent) instead of the Pi (system node).
  - ../../../components/node-selectors/native-cloud
