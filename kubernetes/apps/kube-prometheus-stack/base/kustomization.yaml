apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - helmrelease.yaml
  - externalsecret.yaml
  - externalsecret-thanos-objstore.yaml
  - externalsecret-grafana-admin.yaml
  - dashboard-app-logs.yaml
  - ingress-grafana-lan.yaml
# node-selectors component removed: verified NO-OP here (no controller for it to
# patch — placement comes from helm values / the CNPG Cluster's spec.affinity).
