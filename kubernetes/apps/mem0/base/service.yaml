apiVersion: v1
kind: Service
metadata:
  name: mem0
  namespace: automation
  labels:
    app: mem0
spec:
  selector:
    app: mem0
  ports:
    - name: http
      protocol: TCP
      port: 8000
      targetPort: 8000
  type: ClusterIP
