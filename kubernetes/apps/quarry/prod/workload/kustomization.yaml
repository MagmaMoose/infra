apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# The workload for prod, applied by the `prod-quarry` Flux Kustomization (see
# ../flux-kustomization.yaml). Each resource carries its own metadata.namespace
# (the CNPG Database lives in `database`, the rest in `quarry`).
resources:
  - database.yaml
  - ghcr-pull-secret.yaml
  - externalsecret.yaml
  - helmrelease.yaml
  # ingress.yaml intentionally omitted until the Cloudflare Access policy in
  # terraform/cloudflare/zero-trust/prod exists -- add it back with that change.
  # - ingress.yaml
