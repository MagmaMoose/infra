apiVersion: v1
kind: Service
metadata:
  name: homeassistant
  namespace: automation
spec:
  selector:
    app: homeassistant
  ports:
    - name: web-ui
      protocol: TCP
      port: 8123
      targetPort: 8123