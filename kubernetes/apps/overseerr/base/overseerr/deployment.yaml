apiVersion: apps/v1
kind: Deployment
metadata:
  name: overseerr
  namespace: media
spec:
  replicas: 1
  selector:
    matchLabels:
      app: overseerr
  template:
    metadata:
      labels:
        app: overseerr
    spec:
      automountServiceAccountToken: false
      containers:
        - name: overseerr
          image: sctx/overseerr:1.35.0 # {"$imagepolicy": "flux-system:overseerr"}
          ports:
            - containerPort: 5055
          env:
            - name: TZ
              value: "Europe/Amsterdam"
            - name: LOG_LEVEL
              value: "info"
            - name: PORT
              value: "5055"
          volumeMounts:
            - mountPath: /app/config
              name: overseerr-config
          resources:
            requests:
              # KRR-sized (2026-07): cpu 10m, memory 174Mi. Limit kept generous to avoid OOM.
              memory: "174Mi"
              cpu: "10m"
              ephemeral-storage: "1Gi"
            limits:
              memory: "512Mi"
              ephemeral-storage: "2Gi"
      volumes:
        - name: overseerr-config
          persistentVolumeClaim:
            claimName: overseerr-config
