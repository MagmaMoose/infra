---
apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImageRepository
metadata:
  name: caldrith
  namespace: flux-system
spec:
  image: ghcr.io/magmamoose/caldrith
  interval: 5m
  provider: generic
  secretRef:
    name: ghcr-pull-secret
---
apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: caldrith
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: caldrith
  filterTags:
    pattern: '^v[0-9]+\.[0-9]+\.[0-9]+$'
    extract: '$0'
  policy:
    semver:
      range: '>=0.1.0'
---
# Flux rewrites the tag in the caldrith repo's k8s/overlays/prod/
# kustomization.yaml against the `$imagepolicy` marker and pushes the bump
# back to that repo's main branch.
apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImageUpdateAutomation
metadata:
  name: caldrith
  namespace: flux-system
spec:
  interval: 5m
  sourceRef:
    kind: GitRepository
    name: caldrith
  git:
    checkout:
      ref:
        branch: main
    commit:
      author:
        email: actions@users.noreply.github.com
        name: fluxcdbot
      messageTemplate: |-
        chore: update caldrith image
        {{range .Changed.Changes}}{{print .OldValue}} -> {{println .NewValue}}{{end}}

        [ci skip]
    push:
      branch: main
  update:
    path: "./k8s/overlays/prod"
    strategy: Setters
