apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# The copy from MinIO is NOT here but in kubernetes/apps/seaweedfs-migration, because it runs
# for hours and this Kustomization waits for everything in it to be healthy.
resources:
  - externalsecret.yaml
  - statefulset.yaml
  - service.yaml
  - buckets-job.yaml
