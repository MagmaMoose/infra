apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - pv.yaml
  - pvc.yaml
  - daemonset.yaml
# Inlined from the former clusters/firefly/backup/timemachine overlay.
components:
  # No resource-profile component: KRR-derived values live inline in daemonset.yaml.
  # t.small used to `op: add` over containers/0/resources, silently discarding them.
  - ../../../../components/node-selectors/mini
