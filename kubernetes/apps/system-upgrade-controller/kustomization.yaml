apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Only env overlays here — ./base is applied exclusively by the per-env Flux
# Kustomization, same convention as node-tuning.
resources:
  - ./prod
