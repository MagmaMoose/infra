apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Upstream v0.20.1, vendored verbatim. Do NOT hand-edit crd.yaml / controller.yaml —
# re-download them from the release so an upgrade is a clean file swap.
resources:
  - crd.yaml
  - controller.yaml
