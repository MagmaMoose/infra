apiVersion: v1
kind: Service
metadata:
  name: ollama-lan
  namespace: automation
  labels:
    app: ollama-lan
spec:
  ports:
    - name: http
      protocol: TCP
      port: 11434
      targetPort: 11434
  type: ClusterIP
