apiVersion: v1
kind: Service
metadata:
  name: excalidraw
  namespace: misc
spec:
  selector:
    app: excalidraw
  ports:
    - name: web
      protocol: TCP
      port: 80
      targetPort: 80
    - name: api
      protocol: TCP
      port: 4000
      targetPort: 4000
